{
    "name": "lotto-pcso-scraper",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Playwright sidecar service that scrapes pcso.gov.ph/searchlottoresult.aspx and returns parsed JSON to Laravel. Lives outside the main app build so its Chromium dep doesn't bloat the Vite bundle.",
    "scripts": {
        "start": "node server.mjs",
        "smoke": "node -e 'import(\"./browser.mjs\").then(m => m.getLatestResults({refresh:true})).then(r => console.log(JSON.stringify(r, null, 2))).catch(e => { console.error(e); process.exit(1); })'"
    },
    "engines": {
        "node": ">=20"
    },
    "dependencies": {
        "express": "^4.21.0",
        "playwright": "^1.49.0"
    }
}
